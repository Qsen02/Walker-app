import { StyleSheet } from "react-native";

export const addWaterStyles=StyleSheet.create({
    buttonWrapper:{
        display:"flex",
        flexDirection:"row",
        justifyContent:"center",
        gap:30
    }
})